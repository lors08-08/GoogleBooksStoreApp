import { TCategory, TOrderBy } from '@src/entities/BooksList/types';

export const CATEGORIES: TCategory[] = [
  'all',
  'art',
  'biography',
  'computers',
  'history',
];
export const ORDER_BY: TOrderBy[] = ['newest', 'relevance'];
